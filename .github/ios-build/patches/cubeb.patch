diff --git a/CMakeLists.txt b/CMakeLists.txt
index 1b18c9d..b3c708b 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -145,6 +145,12 @@ target_compile_definitions(speex PRIVATE RANDOM_PREFIX=speex)
 include(CheckIncludeFiles)
 
 check_include_files(AudioUnit/AudioUnit.h USE_AUDIOUNIT)
+# This bundled AudioUnit backend uses macOS-only CoreAudio device enumeration APIs
+# (AudioObjectPropertyAddress, kAudioHardwareProperty*, ...) which do not exist on iOS,
+# so it is not built for the iOS port (cubeb falls back to a silent context there).
+if(EKA2L1_IOS)
+  set(USE_AUDIOUNIT FALSE)
+endif()
 if(USE_AUDIOUNIT)
   target_sources(cubeb PRIVATE
     src/cubeb_audiounit.cpp
